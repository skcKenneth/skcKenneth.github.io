/* Authored foundation lessons and controlled contrasts. No generated prose or remote service. */
const LearningContent = (() => {
  const chapters = {}, comparisons = {};
  const n = x => Number(x.toFixed(8)).toString().replaceAll('-', '−');
  function rng(seed) { let x=seed>>>0; return {int(a,b){x=(Math.imul(x,1664525)+1013904223)>>>0;return a+Math.floor(x/4294967296*(b-a+1));}}; }
  const step=(doing,why)=>({doing,why});
  const ex=(prompt,answer,steps,hint,params)=>({prompt,answer,steps,hint,params});
  function section(lesson,id,title,concepts,diagram,make){
    (chapters[lesson]??=[]).push({id,title,concepts,diagram,make(seed){return make(rng(seed));}});
  }
  section('operations','signed-add','加減：先分清方向，再合併數量',[
    '正、負號可以表示相反方向。例如收入為正、支出為負。兩個負數相加，就是把兩筆支出合起來，總支出變大。',
    '異號相加時，先抵銷同樣多的正、負部分，再看剩下哪個方向。不是見到負號就把所有數字相加。',
    '減去一個數，等於加上它的相反數。尤其減去負數時，要把「減法符號」與「負數本身的符號」分開看。'
  ],'原有量 → 加上變化 → 新的量；減去 −b = 加上 b',r=>{
    const a=r.int(3,18),b=r.int(2,15),c=r.int(1,10);
    return ex(`計算 −${a}−(−${b})+${c}。`,-a+b+c,[step(`−${a}−(−${b}) = −${a}+${b}。`,'減去負數，等於加上它的相反數。'),step(`先合併正數：${b}+${c}=${b+c}。`,'都是正方向的量，可以先合併。'),step(`−${a}+${b+c}=${n(-a+b+c)}。`,'正負相抵後，按剩餘的方向決定符號。')],'先將減法改為加相反數。',{a,b,c});
  });
  section('operations','power-base','乘方：先找完整的底數',[
    'a² 表示 a×a，平方作用在底數上。有括號時，括號內的整個量就是底數。',
    '−a² 表示 a 平方後的相反數；(−a)² 則把負數 −a 乘兩次。書寫時不能省略決定底數的括號。',
    '負數乘負數得正，所以負數的平方是正數。負數三次方含三個負因數，結果為負；不可把「負數乘方」一概視為正。'
  ],'−a²：a → 平方 → 取相反數　　(−a)²：−a → 乘自己',r=>{
    const a=r.int(2,10),b=r.int(1,15);
    return ex(`計算 (−${a})²−${b}。`,a*a-b,[step(`底數是整個 −${a}。`,'括號把負號與數值包在同一個底數內。'),step(`(−${a})×(−${a})=${a*a}。`,'兩個負因數相乘得正。'),step(`${a*a}−${b}=${n(a*a-b)}。`,'先完成乘方，最後做減法。')],'把平方寫成兩個相同因數相乘。',{a,b});
  });
  section('operations','order','混合運算：括號、乘方與同級次序',[
    '先算括號內的內容，再做乘方，然後乘除，最後加減。每做一步都把其餘部分原樣抄下，能減少漏項。',
    '乘法與除法同級，要由左至右。加法與減法也同級；「先乘除」不等於所有乘法都先於除法。',
    '運算次序不同可能得到不同結果。用括號表示整體時，要知道它改變了哪個運算的輸入，不能為了方便隨意加括號。'
  ],'括號 → 乘方 → 乘除（左至右）→ 加減（左至右）',r=>{
    const a=r.int(2,8),b=r.int(2,9),c=r.int(2,6),d=r.int(1,10);
    return ex(`計算 ${a*b}÷${b}×${c}−${d}。`,a*c-d,[step(`${a*b}÷${b}=${a}，留下 ${a}×${c}−${d}。`,'除法和乘法同級，先算左邊的除法。'),step(`${a}×${c}=${a*c}。`,'乘法先於末尾的減法。'),step(`${a*c}−${d}=${n(a*c-d)}。`,'最後相減；沒有改動原來的括號結構。')],'不要先把除數和後面的乘數相乘。',{a,b,c,d});
  });
  section('operations','scientific','大數與近似數：保留數值，交代精度',[
    '科學記數法 a×10ⁿ 把有效數字與數量級分開。非零數的首因數滿足 1≤|a|<10；移動小數點後要用 10 的冪補償。',
    '正的大數改寫時，小數點向左移 n 位，就乘 10ⁿ。例如 45000=4.5×10⁴，而不是 45×10⁴。',
    '近似數不是精確值。按指定數位四捨五入時，查看下一位；例如 3.146 保留兩位小數為 3.15。題目沒有要求近似時，先保留精確數值。'
  ],'45000 → 4.5（縮小 10000 倍）→ ×10⁴（補回原值）',r=>{
    const a=r.int(2,9),p=r.int(3,7),value=a*10**p;
    return ex(`把 ${value} 寫成 a×10ⁿ，其中 1≤a<10。求指數 n。`,p,[step(`首因數取 ${a}。`,'它介乎 1 與 10 之間，符合規範。'),step(`${value}÷${a}=${10**p}=10^${p}。`,'用除法確定要補償的 10 的冪。'),step(`所以 ${value}=${a}×10^${p}，n=${p}。`,'乘回去應得到原數。')],'數值變小後，要用相應的 10 的冪補回。',{a,p});
  });
  section('rational','classification','正負數、零與共同基準',[
    '整數和分數合稱有理數。有限小數及循環小數也能寫成分數，例如 0.5=1/2；數字的書寫形式與數的分類要分開。',
    '正負號表示相對同一基準的相反方向。0 既不是正數也不是負數，但它是整數，也是有理數。',
    '記錄溫差、高度或收支時，先說明零點與正方向。負值不是「沒有」，而是位於基準的另一側。'
  ],'低於基準 ← 負數 ← 0 → 正數 → 高於基準',r=>{
    const base=r.int(10,30),below=r.int(1,9);
    return ex(`以 ${base}°C 為基準，某溫度記作 −${below}°C 的偏差。實際溫度是多少 °C？`,base-below,[step(`−${below} 表示比基準低 ${below}°C。`,'偏差的負號表示方向，不是實際溫度的符號。'),step(`實際溫度=${base}+(−${below})。`,'基準加偏差，才是原來的測量值。'),step(`實際溫度=${base-below}°C。`,'可再減去基準，核對偏差是負值。')],'先分清基準與偏差。',{base,below});
  });
  section('rational','line','數軸、大小與距離',[
    '數軸要有原點、正方向和單位長度。位置越靠右，表示的數越大，不能只比較寫出來的數字大小。',
    '兩個負數中，離原點較遠的更靠左，因而較小。例如 −7<−3，雖然 7>3。',
    '兩點間的距離是非負數，等於右端坐標減左端坐標。跨過原點時，可把兩段距離相加。'
  ],'← −7 —— −3 —— 0 —— 3 →；向右數值增加',r=>{
    const a=r.int(2,15),b=r.int(1,15);
    return ex(`數軸上 A=−${a}、B=${b}。AB 長多少個單位？`,a+b,[step(`A 到原點的距離是 ${a}。`,'距離不帶方向，所以不寫負號。'),step(`原點到 B 的距離是 ${b}。`,'原點位於 A、B 之間。'),step(`AB=${a}+${b}=${a+b}。`,'兩段正距離相加，也等於右端減左端。')],'可先在紙上畫出 A、0、B 的次序。',{a,b});
  });
  section('rational','opposite','相反數與絕對值',[
    '相反數是數軸上關於原點對稱的數；兩個相反數相加為零。正數的相反數為負，負數的相反數為正，0 的相反數仍是 0。',
    '絕對值是到原點的距離，只能非負。|a| 不是把任何式子裡的所有負號刪掉，而是先求出式子的值，再量度距離。',
    '知道絕對值通常未能確定原數。例如 |x|=4 有 x=4 或 x=−4，還需要符號條件來選擇。'
  ],'−a ← 對稱於 0 → a；兩點的絕對值相同',r=>{
    const a=r.int(2,18),b=r.int(1,9);
    return ex(`已知 x<0 且 |x|=${a}，求 x 的相反數加 ${b}。`,a+b,[step(`|x|=${a} 給出 x=±${a}。`,'原點左右各有一個符合距離的點。'),step(`因 x<0，x=−${a}，其相反數是 ${a}。`,'先用條件確定原數，再反向。'),step(`${a}+${b}=${a+b}。`,'最後完成題目要求的加法。')],'相反數與絕對值是兩個不同操作。',{a,b});
  });
  section('expressions','translate','把語句寫成式子',[
    '字母代表一個可變或未知的數。2x 表示 2×x；x 的 2 倍與 x 加 2 並不相同。',
    '先辨認每個量的意思，再按語句的先後關係列式。「先減後乘」需要用括號保留整體。',
    '每件單價要乘件數，一次性的費用只加一次。式子中的每一部分應能對應到情境中的數量。'
  ],'件數 × 單價 ＋ 一次固定費 = 總費用',r=>{
    const a=r.int(2,12),b=r.int(2,15),x=r.int(3,12);
    return ex(`每本簿 ${a} 元，一單另付 ${b} 元包裝費。買 ${x} 本共付多少元？`,a*x+b,[step(`買 x 本的簿本費是 ${a}x。`,'每本都付相同單價，所以乘本數。'),step(`總費用 C=${a}x+${b}。`,'包裝費每單一次，不乘本數。'),step(`代 x=${x}，C=${a*x+b} 元。`,'把字母換成題目給的本數。')],'先把一次費用和每件費用分開。',{a,b,x});
  });
  section('expressions','substitute','代入：把字母整個換成數值',[
    '求式子的值不是解方程；字母的值已由題目給定。每個相同字母都要代入同一個數。',
    '負數代入時加括號，尤其遇到乘方或減法。例如 x=−2 時，x²=(−2)²=4。',
    '代入後仍依運算次序計算。可以先估計結果符號或大小，再核對細算結果。'
  ],'x → (給定數值) → 先乘方 → 再乘除加減',r=>{
    const a=r.int(2,8),b=r.int(1,12),x=-r.int(2,10);
    return ex(`x=${n(x)} 時，求 ${a}x²−${b}。`,a*x*x-b,[step(`原式=${a}×(${n(x)})²−${b}。`,'負號屬於 x 的值，必須放在底數括號內。'),step(`(${n(x)})²=${x*x}，再乘 ${a} 得 ${a*x*x}。`,'先乘方後乘法。'),step(`${a*x*x}−${b}=${a*x*x-b}。`,'最後減常數項。')],'不要把 x² 寫成負號在平方外的式子。',{a,b,x});
  });
  section('expressions','pattern','從增加量建立規律',[
    '找規律時，先列少量具體例子，再比較每次增加的量，最後說明這個增加量為甚麼固定。',
    '相鄰正方形共用一條邊，第一個用 4 根火柴，此後每增加一格只添 3 根。',
    '式子 4+3(n−1) 把「第一格」與「其餘 n−1 格」分開。代入 n=1 能檢查有沒有多算或少算起點。'
  ],'格數：1 → 2 → 3　火柴：4 → 7 → 10（每次加 3）',r=>{
    const count=r.int(4,25);
    return ex(`一列 ${count} 個相鄰正方形共用邊，需要幾根火柴？`,3*count+1,[step('第一個用 4 根。','它沒有與之前的圖形共用邊。'),step(`其餘 ${count-1} 個各增加 3 根。`,'每個新圖形已有左邊，只補上、下、右。'),step(`4+3×${count-1}=${3*count+1} 根。`,'合併初始量與後來增加量。')],'不要把每一格都乘 4。',{count});
  });
  section('polynomial-add','terms','單項式、次數與同類項',[
    '單項式由數與字母相乘而成，數字部分是係數。次數是所有字母指數的總和；例如 −2x²y 的係數是 −2，次數是 3。',
    '多項式由多個單項式相加而成，每一項連同前面的符號一起讀。常數項沒有字母。',
    '同類項要求字母相同，而且各相同字母的指數相同。係數可以不同；總次數相同並不足夠，例如 x²y 與 xy² 不是同類項。'
  ],'−2x²y：係數 −2 ｜ x 指數 2 ｜ y 指數 1 ｜ 次數 3',r=>{
    const a=r.int(2,12),u=r.int(2,8),v=r.int(1,8);
    return ex(`單項式 −${a}x^${u}y^${v} 的次數是多少？`,u+v,[step(`x 的指數為 ${u}，y 的指數為 ${v}。`,'次數只計字母的指數。'),step(`係數 −${a} 不計入次數。`,'數字的大小與符號不決定單項式次數。'),step(`次數=${u}+${v}=${u+v}。`,'把各字母的指數相加。')],'不要把係數也加進去。',{a,u,v});
  });
  section('polynomial-add','combine','合併同類項：只改係數',[
    '把同類項想像成同一種數量：3x+2x=(3+2)x=5x。這是分配律的逆用。',
    '合併時只相加係數，字母和指數保留不變。x²+x²=2x²，不是 x⁴；x² 與 x 則不能合併。',
    '整理後可以代入一個數檢查。代值檢查能發現錯誤，但一次吻合不能取代一般性的代數推理。'
  ],'3x ＋ 2x = (3＋2) 個 x = 5x',r=>{
    const a=r.int(2,9),b=r.int(1,8),c=r.int(1,12),x=r.int(-7,7);
    return ex(`化簡 ${a}x−${b}x+${c}，再代 x=${n(x)} 求值。`,(a-b)*x+c,[step(`${a}x−${b}x=(${a}−${b})x。`,'兩項有同一字母部分，係數相減。'),step(`原式=(${a-b})x+${c}。`,'常數項與 x 項不同類，分開保留。'),step(`代 x=${n(x)}，值為 ${n((a-b)*x+c)}。`,'先乘係數，再加常數。')],'把每項前面的符號連同係數一起讀。',{a,b,c,x});
  });
  section('polynomial-add','brackets','負括號：每一項都要變號',[
    '括號前是加號，可以直接去括號；前面是負號，相當於用 −1 乘括號內每一項。',
    '例如 −(2x−3)=−2x+3。只把首項變號，會令整個式子不再等值。',
    '有多層括號時，一次處理一層，再合併同類項。先保留完整中間式，熟練後才簡化書寫。'
  ],'−(A−B) = (−1)A ＋ (−1)(−B) = −A＋B',r=>{
    const a=r.int(3,12),b=r.int(1,8),c=r.int(1,12),x=r.int(-5,8);
    return ex(`求 ${a}x−(${b}x−${c}) 在 x=${n(x)} 時的值。`,(a-b)*x+c,[step(`去括號：${a}x−${b}x+${c}。`,'負號要同時乘到字母項和常數項。'),step(`合併得 (${a-b})x+${c}。`,'只合併同類項。'),step(`代值後得 ${n((a-b)*x+c)}。`,'代入時保留負數括號，可再用原式驗算。')],'先寫出負括號展開的完整一行。',{a,b,c,x});
  });
  section('linear-equation','balance','等式：兩邊保持平衡',[
    '方程是含未知數的等式；方程的解是代入後使兩邊相等的數。一元一次方程整理後有非零的一次項係數。',
    '等式兩邊同加、同減一個數，仍相等；同乘或同除非零數，也保留相等關係。除以零沒有定義。',
    '「移項變號」是兩邊同做運算的簡寫。先理解兩邊發生甚麼，再使用簡寫，才不會任意改符號。'
  ],'左邊 = 右邊 → 兩邊同減 b → 兩邊同除 a（a≠0）',r=>{
    const a=r.int(2,9),b=r.int(1,20),x=r.int(-8,12),rhs=a*x+b;
    return ex(`解 ${a}x+${b}=${n(rhs)}。`,x,[step(`兩邊同減 ${b}，得 ${a}x=${n(a*x)}。`,'把常數項移除，同時保留等式平衡。'),step(`兩邊同除 ${a}，得 x=${n(x)}。`,'x 的係數要由原來的數變成 1。'),step(`代回：${a}×(${n(x)})+${b}=${n(rhs)}。`,'原方程成立，解才通過檢驗。')],'每一步都說出等式兩邊同做哪個運算。',{a,b,x,rhs});
  });
  section('linear-equation','fraction','去括號、去分母：作用於每一項',[
    '去括號依分配律處理；去分母則把等式兩邊整體乘上共同分母。兩者都要處理每一項。',
    '例如 (x+2)/3−1=4 兩邊乘 3，得到 x+2−3=12；原本沒有分母的 −1 也要乘 3。',
    '整理後再解一次方程，最後代回原來帶分母的方程檢查，這能找出漏乘或符號錯誤。'
  ],'共同分母 × 左邊每一項 = 共同分母 × 右邊每一項',r=>{
    const d=r.int(2,8),b=r.int(1,12),c=r.int(1,8),rhs=r.int(2,15),x=d*(rhs+c)-b;
    return ex(`解 (x+${b})/${d}−${c}=${rhs}。`,x,[step(`兩邊乘 ${d}：x+${b}−${d*c}=${d*rhs}。`,'分母約去，但無分母的常數也要乘。'),step(`兩邊加 ${d*c}、減 ${b}，得 x=${x}。`,'把 x 以外的量從左邊移除。'),step(`檢查 (${x}+${b})/${d}−${c}=${rhs}。`,'代回原式，而不是只檢查已變形的式子。')],'先在每一項上標出要乘的共同分母。',{d,b,c,rhs,x});
  });
  section('linear-equation','model','應用題：先寫等量關係',[
    '先設未知數並說明單位，再用一句話寫等量關係。例如「商品費＋固定費＝實付總額」。',
    '方程的每一項都要對應情境。百分率要辨認基準，路程題要辨認共同起點及速度方向。',
    '解出數值後，檢查是否符合情境：本數要非負整數，長度與時間不能是負數，還要回答題目所求。'
  ],'設未知量 → 找等量關係 → 列方程 → 求解 → 回到情境',r=>{
    const a=r.int(2,10),fee=r.int(3,20),x=r.int(3,20),total=a*x+fee;
    return ex(`每本簿 ${a} 元，包裝 ${fee} 元，實付 ${total} 元。買了幾本？`,x,[step(`設買 x 本，列 ${a}x+${fee}=${total}。`,'總額由簿本費與一次包裝費組成。'),step(`扣除包裝費後，${a}x=${a*x}，所以 x=${x}。`,'先扣固定部分，再除單價。'),step(`檢查 ${a}×${x}+${fee}=${total}，答 ${x} 本。`,'解符合非負整數及原本費用條件。')],'先把總額中不隨本數改變的費用扣除。',{a,fee,x,total});
  });
  section('inequality','direction','不等號何時要反向？',[
    '不等式的解集包含所有令條件成立的數，通常不只一個。x≤3 包括 3 以及所有更小的數。',
    '兩邊同加減一個數，或同乘除正數，方向不變；同乘除負數才需要反轉不等號。',
    '可用數軸想像：乘 −1 把左右對調。2<3 變成 −2>−3；不要把「移項」與「乘除負數」混為一談。'
  ],'2 < 3 → 同乘 −1 → −2 > −3（左右反向）',r=>{
    const a=r.int(2,10),b=r.int(-10,10);
    return ex(`−${a}x>${n(-a*b)} 可寫成 x<k。求 k。`,b,[step(`兩邊都除以 −${a}。`,'要把 x 的係數變成 1。'),step('> 改為 <。','除數為負數，數的次序反轉。'),step(`x<${n(b)}，故 k=${n(b)}。`,'右邊也要除以同一個數。')],'先檢查除數的符號。',{a,b});
  });
  section('inequality','intersection','多個條件：找公共部分',[
    '不等式組要求每一條同時成立，取解集的交集。「或」只需其中一條成立，與「同時」的意思不同。',
    '數軸上空心點表示端點不包含，實心點表示包含。先決定端點，再看向左或向右延伸。',
    '若要求整數解，從符合條件的第一個整數列到最後一個。兩端都計入時，個數是最大減最小再加 1。'
  ],'x>a：a ○──→　x≤b：←──● b　交集：a ○──● b',r=>{
    const a=r.int(-8,5),b=a+r.int(3,12);
    return ex(`滿足 ${n(a)}<x≤${b} 的整數有幾個？`,b-a,[step(`左端不含 ${n(a)}，首個整數是 ${n(a+1)}。`,'嚴格小於不包含等號端點。'),step(`右端含 ${b}，它是最後一個整數。`,'≤ 包含等號情況。'),step(`個數=${b}−(${n(a+1)})+1=${b-a}。`,'加 1 將首項也計入。')],'先在紙上畫空心及實心端點。',{a,b});
  });
  section('inequality','application','最多與至少：不等式回到情境',[
    '「最多、不超過」通常用 ≤；「至少、不少於」通常用 ≥。先確定限制的是費用、件數還是其他量。',
    '數學解集容許小數，但情境未必容許。購買件數要取整數；求最多時向下取，求至少達標時可能要向上取。',
    '把所選整數及相鄰的一個整數代回限制，可以檢查是否真的達到最大或最小要求。'
  ],'固定費 ＋ 單價 × 件數 ≤ 預算 → 取可行整數',r=>{
    const a=r.int(3,12),fee=r.int(3,20),count=r.int(3,20),extra=r.int(1,a-1),budget=fee+a*count+extra;
    return ex(`預算 ${budget} 元，固定費 ${fee} 元，每件 ${a} 元。最多買幾件？`,count,[step(`${fee}+${a}x≤${budget}。`,'支出可以低於預算，不要求剛好相等。'),step(`x≤${budget-fee}/${a}；上界約為 ${n((budget-fee)/a)}。`,'先扣固定費，再除正單價，方向不變。'),step(`最多 ${count} 件，多一件要 ${fee+a*(count+1)} 元，超出預算。`,'件數是整數，不能四捨五入到不符合限制的值。')],'求最多時，選不超過上界的整數。',{a,fee,count,extra,budget});
  });

  const pair=(title,make)=>({title,make});
  const side=(prompt,answer,working)=>({prompt,answer,working});
  function contrast(title,left,right,rows,explanation){return {title,left,right,rows,explanation};}
  comparisons.operations=[
    pair('括號與乘方',seed=>{const a=2+(seed%8),p=seed%3===2?3:2;return contrast(`比較${p===2?'平方':'三次方'}：先找底數`,side(`−${a}${p===2?'²':'³'}`,-(a**p),`−(${Array(p).fill(a).join('×')})=${n(-(a**p))}`),side(`(−${a})${p===2?'²':'³'}`,(-a)**p,`${Array(p).fill('(−'+a+')').join('×')}=${n((-a)**p)}`),[['底數',String(a),'−'+a],['負號的位置','乘方外','底數內']],p===2?'平方時，負號是否包含在底數會改變結果。':'三次方時兩式同值，但底數結構仍不同；不能只靠一次答案相同就說結構相同。');}),
    pair('減去負數與加上負數',seed=>{const a=3+seed%15,b=2+seed%8;return contrast('同樣的負數，前面的運算不同',side(`${a}−(−${b})`,a+b,`${a}+${b}=${a+b}`),side(`${a}+(−${b})`,a-b,`${a}−${b}=${n(a-b)}`),[['動作','加上負數的相反數','加上一個負的變化'],['數軸方向','向右 '+b,'向左 '+b]],'「減去負數」向右移，「加上負數」向左移。看清運算符號與數本身的符號。');})
  ];
  comparisons.rational=[
    pair('相反數與絕對值',seed=>{const a=(seed%2? -1:1)*(2+seed%18);return contrast(`對 ${n(a)} 做兩個不同操作`,side(`${n(a)} 的相反數`,-a,`${n(a)}+(${n(-a)})=0`),side(`|${n(a)}|`,Math.abs(a),`距原點 ${Math.abs(a)} 個單位`),[['意義','關於原點對稱','到原點的距離'],['結果限制','可以正、負或零','必須非負']],a>0?'正數的相反數是負，絕對值仍是正。':'負數的相反數和絕對值恰好同值，但兩個操作的意思不同；換正數就能分辨。');}),
    pair('負數與絕對值的大小',seed=>{const a=2+seed%15,b=a+1+seed%9;return contrast('比較原數與距離',side(`−${a} 與 −${b} 中較大的數`,-a,`−${b}<−${a}`),side(`|−${a}| 與 |−${b}| 中較大的值`,b,`${a}<${b}`),[['比較對象','數軸上的左右位置','到原點的距離'],['判準','越右越大','越遠越大']],'負數離原點越遠，位置越靠左，所以數值越小。');})
  ];
  comparisons.expressions=[
    pair('先乘後減與先減後乘',seed=>{const a=2+seed%5,b=2+seed%8,x=b+3;return contrast(`x=${x}：括號改變運算對象`,side(`${a}x−${b}`,a*x-b,`${a}×${x}−${b}=${a*x-b}`),side(`${a}(x−${b})`,a*(x-b),`${a}×(${x}−${b})=${a*(x-b)}`),[['先做','x 乘 '+a,'x 減 '+b],['乘法對象','x','整個差']],'先按語句決定式子；代入後依括號所定的次序計算。');}),
    pair('固定費與每件費',seed=>{const x=2+seed%10,a=3+seed%7,b=2+seed%9;return contrast(`買 ${x} 件，每件基本費 ${a} 元`,side(`整單另加 ${b} 元`,a*x+b,`${a}×${x}+${b}=${a*x+b}`),side(`每件另加 ${b} 元`,(a+b)*x,`(${a}+${b})×${x}=${(a+b)*x}`),[['額外費次數','1 次',x+' 次'],['固定部分',b+' 元','沒有整單固定費']],'「整單」只加一次，「每件」需要乘件數。');})
  ];
  comparisons['polynomial-add']=[
    pair('負括號與無括號',seed=>{const a=5+seed%8,b=2+seed%3,c=1+seed%12,x=2;return contrast('x=2：減號作用到哪裡？',side(`${a}x−(${b}x−${c})`,(a-b)*x+c,`${a}x−${b}x+${c} → ${(a-b)*x+c}`),side(`${a}x−${b}x−${c}`,(a-b)*x-c,`(${a-b})x−${c} → ${n((a-b)*x-c)}`),[['常數項','+'+c,'−'+c],['負號作用','括號內全部項','只有緊接的一項']],'加減法的括號不能隨意刪去；負括號展開要逐項變號。');}),
    pair('同類項與不同類項',seed=>{const a=2+seed%8,b=3+seed%7,x=2+seed%5;return contrast(`x=${x}：先判斷能否合併`,side(`${a}x²+${b}x²`,(a+b)*x*x,`(${a+b})x² → ${(a+b)*x*x}`),side(`${a}x²+${b}x`,a*x*x+b*x,`兩項不同類；代入得 ${a*x*x+b*x}`),[['字母指數','兩項都是 2','分別是 2 與 1'],['化簡','相加係數','保留兩項']],'不能因為都有 x 就合併。答案欄填代值結果，先在紙上寫出化簡式。');})
  ];
  comparisons['linear-equation']=[
    pair('兩邊同做與只做一邊',seed=>{const a=2+seed%8,b=2+seed%12,x=3+seed%10,c=a*x+b;return contrast(`原方程 ${a}x+${b}=${c}；以下哪個變形保留原解？先各自求解。`,side(`${a}x=${c-b}`,x,`兩邊同減 ${b}，x=${x}`),side(`${a}x=${c}`,c/a,`只改左邊，x=${c}/${a}`),[['是否保留等價','是','否，已變成另一個方程'],['操作','左右各減 '+b,'只有左邊減 '+b]],'右欄是常見錯誤變形，雖然也能求出數值，卻不是原方程的解。');}),
    pair('去分母有沒有乘遍每項',seed=>{const d=2+seed%6,b=2+seed%8,c=1+seed%5,k=3+seed%7;return contrast(`原方程 (x+${b})/${d}−${c}=${k}；比較兩個變形，各求 x。`,side(`x+${b}−${d*c}=${d*k}`,d*(k+c)-b,`每項乘 ${d} → x=${d*(k+c)-b}`),side(`x+${b}−${c}=${d*k}`,d*k+c-b,`漏乘 −${c} → x=${d*k+c-b}`),[['常數的變化','−'+c+' 變 −'+d*c,'仍是 −'+c],['等價性','保留原解','改變了原式']],'去分母是整個等式兩邊同乘，不是只刪去看見的分母。');})
  ];
  comparisons.inequality=[
    pair('正係數與負係數',seed=>{const a=2+seed%9,b=2+seed%15;return contrast('兩個解集的界限各是多少？另留意方向。',side(`${a}x<${a*b}，解為 x<k；填 k`,b,`除以正數 ${a}：x<${b}`),side(`−${a}x<${a*b}，解為 x>k；填 k`,-b,`除以負數 −${a}：x>−${b}`),[['除數符號','正','負'],['不等號','保留 <','反轉為 >']],'同樣是除法，除數的符號決定是否反向。只填界限值不代表已理解方向，請讀出完整解集。');}),
    pair('嚴格與包含端點',seed=>{const a=seed%8-3,b=a+3+seed%12;return contrast('計算兩個區間的整數個數',side(`${n(a)}<x<${b}`,b-a-1,`${n(a+1)} 至 ${b-1}，共 ${b-a-1} 個`),side(`${n(a)}<x≤${b}`,b-a,`${n(a+1)} 至 ${b}，共 ${b-a} 個`),[['右端點','空心，不包含 '+b,'實心，包含 '+b],['共同左端點','不包含 '+n(a),'不包含 '+n(a)]],'只改一個等號，就多包含右端點這一個整數。');})
  ];
  return {chapters,comparisons,rng};
})();
